# Ultralytics dataset config (container-internal paths)
path: /datasets/my_dataset
train: train/images
val: val/images

nc: 2
# Class index order — must match the model's training. The included best.pt
# was trained with 0=black, 1=silver.
names: ['black', 'silver']
