# Dataset config for YOLOv8n training.
# `path` is filled in (absolute) by `k230_pipeline.py organize`, which also
# rewrites nc/names from the source classes.txt. Edit by hand if needed.
path: ./datasets/victim_20260621
train: train/images
val: val/images

nc: 3
names:
  0: Dead
  1: Live
  2: Point
