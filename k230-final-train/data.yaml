# Template / fallback only. The REAL data.yaml is generated per dataset by
# `add_data.ps1` (organize) at datasets/<Name>/data.yaml — the scripts always
# pass that one explicitly. Edit here only if you call the CLI without --data.
path: /workspace/datasets/victim
train: train/images
val: val/images

nc: 3
names:
  0: Dead
  1: Live
  2: Point
